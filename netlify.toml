[build]
  command = "pnpm prisma generate && pnpm prisma db push && pnpm build"
  publish = ".next"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "RAZORPAY_KEY_ID"

[[plugins]]
  package = "@netlify/plugin-nextjs"